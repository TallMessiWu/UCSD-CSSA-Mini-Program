<view class="container">
    <image class="thumbnail" src="{{image}}" mode="aspectFill"></image>
    <view class="text-container">
        <text class="title">{{title}}</text>
        <text wx:if="{{summary}}" class="summary">{{summary}}</text>
    </view>

</view>